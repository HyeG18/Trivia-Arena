[package]
name = "api-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.11"
prost = "0.12"
tokio = { version = "1.36", features = ["macros", "rt-multi-thread"] }
tokio-stream = "0.1"
tower = "0.4" # Para balanceo y middleware futuro

[build-dependencies]
tonic-build = "0.11"